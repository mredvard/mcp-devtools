services:
  devtools:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./workspace:/workspace
    environment:
      - PYTHONUNBUFFERED=1
      # - SEARCH_PROVIDER=tavily  # Options: 'searxng' (default), 'tavily'
      # - TAVILY_API_KEY=tvly-YOUR_API_KEY
