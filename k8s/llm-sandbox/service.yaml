apiVersion: v1
kind: Service
metadata:
  name: llm-sandbox
  namespace: llm-sandbox
spec:
  type: LoadBalancer
  selector:
    app: llm-sandbox
  ports:
  - port: 1234
    targetPort: 1234
    protocol: TCP
    name: shell
